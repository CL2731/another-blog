<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> The Blog Station </title>
</head>
<body>
    <header>
        <h1> Welcome to BlogIt </h1>
    </header>
    <nav>
        <a href="/">Home</a>
        {{#if loggedIn}}
        <button id="logout" class="">Logout</button>
        {{else}}
        <a href="/public/js/login.js">Login</a>
        {{/if}}
    </nav>
    <main>
        <aside>
            <h2>Time to Post!</h2>
        </aside>
        <div class="">
            {{body}}
        </div>
    </main>
    {{#if loggedIn}}
    <script src="/public/js/logout.js"></script>
    {{/if}}
</body>
</html>